version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  build:
    commands:
      - ./mvnw clean package
      - docker build -t cicd-demo .

  post_build:
    commands:
      - echo "Deploying to EC2"